enum AppStatus {
  authenticated,
  unAuthenticated,
}

enum LoginStatus {
  inital,
  submiting,
  success,
  error,
}

enum SignupStatus {
  inital,
  submiting,
  success,
  error,
}

enum VerificationcStatus {
  verified,
  notVerified,
}

enum ForgetPasswordStatus {
  inital,
  success,
  error,
}

enum PasswordChangedStatus {
  changed,
  notChanged,
}

enum NavbarItem {
  home,
  library,
  reminders,
  identification,
  community,
}

enum ToolBarItem {
  reminders,
  favorits,
}

enum Status {
  inital,
  looding,
  success,
  error,
}

enum CareType {
  watering,
  fertilizing,
  rotation,
}

enum ResultStatus {
  notStarted,
  notFound,
  found,
}
